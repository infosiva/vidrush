# USER — Who I'm Building For

## Who
Siva. Full-stack developer running a portfolio of ~50+ AI-powered products, solo, across local dev + Vercel. Two Vercel accounts: `infosiva` (frozen, 29 legacy projects) and `sivaprakasam` (all new work — see `CLAUDE.md §U`).

## How I like to work
- Plan before code on anything non-trivial — show the plan, wait for "yes/go/proceed", then execute without re-checking in mid-task.
- Terse responses. Don't narrate steps I can't see the value of. Show results, not process.
- Don't re-explore what's already documented in `CLAUDE.md`, `MEMORY.md`, or `HANDOFF.md` — read those first, always.
- Full pipeline or nothing — a CSS tweak alone is never "the update was made." Same for shipped-but-unverified: build passing locally isn't done until live E2E confirms it.
- Cost-conscious: cheapest model that does the job, free AI providers before paid, disconnect idle tools rather than leave them costing tokens.
- Want a **daily** touchpoint on token/tool hygiene — not just "whenever the nudge fires" — revisit CLAUDE.md's §0-TOKEN-FIX cadence language if it drifts from that.

## Standing preferences
- Git identity for `agents/`: `info.siva@gmail.com` / `Siva` — never a work email.
- Never destructive git ops, never `--no-verify`, without asking first.
- Security: no secrets in code, ever, no hardcoded fallback tokens.
